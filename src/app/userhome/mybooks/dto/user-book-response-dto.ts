import {UserCommonResponseDTO} from "./user-common-response-dto";

/**
 * As of now this is just a marker but in the future it is a possibility that the response could have more fields
 */
export interface UserBookResponseDTO extends UserCommonResponseDTO {
}
